# agents / skills - keep skills tracked for team, ignore local cache
.agents/*
!.agents/skills/
!.agents/skills/**
.claude/
skills-lock.json

# python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# env / secrets — use .env.example as template
.env
*.pem
*.key

# checkpoints / weights (large) — keep folder, ignore binaries until needed
model/checkpoints/*.bin
model/checkpoints/*.safetensors
model/checkpoints/*.pt

# local rulebook / large xlsx not part of submission
aic-rulebook.md

# OS
.DS_Store
.idea/
.vscode/

# local skill symlinks (generated by npx skills)
data/skills/

# demo / large data not needed in git history
Hackvid 24_25.xlsx
